# GitHub Pages Configuration
title: Evolving Agents Labs
description: ai-os — agent work you can check, and prove. Everyone can generate;
  almost nobody can tell you six months later whether the number in their README is
  still the number their code produces.
url: "https://evolvingagentslabs.github.io"
baseurl: ""

# index.html is hand-written static HTML and passes through untouched.
# No layouts, no collections, no includes — nothing here is templated.

exclude:
  - README.md
